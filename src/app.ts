App({
  // @ts-ignore ==> test ts ignore flag
  onLaunch(options) {
    // my.getAuthCode({
    //   scopes: ['offline', 'auth_base', 'auth_user', 'auth_zhima'] as any,
    //   success: (res) => {
    //     console.log(res);
    //   },
    //   fail: () => {},
    // });
  },
  // @ts-ignore ==> test ts ignore flag
  onShow(options) {},
});
